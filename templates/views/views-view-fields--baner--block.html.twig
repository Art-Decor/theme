{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
 */
#}
{% for field in fields -%}
    {%
    set wrapper_classes = [
        field.handler.field == 'field_zdjecie' ? '' : 'container-fluid'
    ]
    %}
    {%
    set field_classes = [
        field.handler.field == 'field_zdjecie' ? 'hidden md:!block absolute inset-0 -z-10',
        field.handler.field == 'field_zdjecie_mobile' ? 'row block md:hidden absolute inset-0 -z-10',
        field.handler.field == 'field_naglowek' ? 'row text-white font-body text-4xl text-[40px] font-light md:text-6xl [&_em]:font-header [&_em]:font-normal mb-10 mt-[140px] md:mt-44 max-w-[555px]',
        field.handler.field == 'field_tekst_pod_naglowkiem' ? 'row text-white font-body text-xl font-normal mb-10',
        field.handler.field == 'field_tekst_przycisku' ? 'row w-fit [&_a]:whitespace-nowrap [&_a]:inline-block [&_a]:text-primary-100 [&_a]:font-body [&_a]:font-medium [&_a]:text-sm [&_a]:bg-white [&_a]:px-6 [&_a]:pr-12 [&_a]:py-5 [&_a]:border [&_a]:border-solid [&_a]:border-white [&_a]:rounded-full [&_a]:hover:text-white [&_a]:hover:bg-transparent [&_a]:transition [&_a]:duration-200',
    ]
    %}
  {%- if field.wrapper_element -%}
    <{{ field.wrapper_element }}{{ field.wrapper_attributes.addClass(wrapper_classes) }}>
  {%- endif %}
  {%- if field.element_type -%}
    <{{ field.element_type }}{{ field.element_attributes.addClass(field_classes) }}>{{ field.content }}</{{ field.element_type }}>
  {%- else -%}
    {{ field.content }}
  {%- endif %}
  {%- if field.wrapper_element -%}
    </{{ field.wrapper_element }}>
  {%- endif %}
{%- endfor %}
